# Properties defining configuration for the Embeddings example, Spring Boot application.

spring.application.name=Embeddings App

spring.ai.ollama.embedding.options.model=nomic-embed-text
spring.ai.openai.embedding.options.model=text-embedding-3-small
